<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description %>
<%= f.association :ingredient, error: "Please pick unique ingredient" %>
<%= f.submit "submit", class: "btn btn-primary" %>
<% end %>
